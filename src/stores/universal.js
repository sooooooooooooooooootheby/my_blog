import { defineStore } from "pinia";

export const universalStore = defineStore({
    id: "universal",
    store: () => {
        return {
            
        };
    },
    actions: {
        
    }
});
